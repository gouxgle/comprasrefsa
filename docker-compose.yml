services:
  web:
    build: .
    container_name: almacenes_web
    restart: unless-stopped
    network_mode: host
    env_file: .env
    volumes:
      - .:/app
    command: gunicorn --config /app/gunicorn.conf.py app:app


